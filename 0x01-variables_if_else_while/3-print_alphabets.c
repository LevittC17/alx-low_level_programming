#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 always if exits correctly
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	for (a = 'A'; a <= 'Z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
