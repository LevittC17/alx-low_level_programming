#include "main.h"

/**
 * print_alphabet - function to print alphabets
 * 
 * Return: 0 always if exits properly
 */
void print_alphabet(void)
{
	char c;
	
	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
_putchar('\n');
return (0);
}
