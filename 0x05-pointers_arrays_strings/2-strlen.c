#include "main.h"

/**
* _strlen - functin to return length of a string
* @s: The parameter
*
* Return: returns length
*/
int _strlen(char *s)
{
	int count;
	
	while (*p != '\0\)
	{
		count++;
		p++;
	}
	return count;
}
