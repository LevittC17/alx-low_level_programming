#include <stdlib.h>
#include <stdio.h>

/**
* printf - print output of argc
*
* Return: On success 0
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	int i;

	printf("argv[%d] = %s\n", i, argv[i]);

	return (0);
}
